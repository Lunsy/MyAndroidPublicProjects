<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.LayoutWorkout" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.LayoutWorkout" parent="Base.Theme.LayoutWorkout" />
    <style name="MyStyle" parent="BackGroundStyle">
        <item name="android:textSize">50sp</item>
        <item name="android:textColor">@color/white</item>

    </style>
    <style name="BackGroundStyle">
        <item name="android:background">@color/darkPurple</item>
    </style>
</resources>