<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:map="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".DriverMapsActivity"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <Button
            android:id="@+id/settings_Driver_Button"
            android:text="Settings"
            android:textSize="14sp"
            android:textStyle="bold"
            android:textColor="@color/purple_darkness"
            android:background="@color/orange_darkness"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"/>

        <Button
            android:id="@+id/sign_out_Driver_Button"
            android:text="Sign Out"
            android:textSize="14sp"
            android:textStyle="bold"
            android:textColor="@color/purple_darkness"
            android:background="@color/orange_darkness"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
                 />
        <!--android:onClick="driverSignOutButton"-->

    </LinearLayout>

<FrameLayout
    android:id="@+id/map"
    android:name="com.google.android.gms.maps.SupportMapFragment"
    android:layout_width="match_parent"
    android:layout_height="match_parent"

    />
</LinearLayout>